import makeStyles from "@mui/styles/makeStyles";

export const useStyles = makeStyles({
  root: {
    width: "100%",
  },
  title: {
    marginBottom: "20px",
  },
  filters: {
    columnGap: "10px",
    display: "flex",
    marginBottom: "32px",
    width: "100%",
  },
});
