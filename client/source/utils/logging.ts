/**
 * Explicitly log a set of arguments to console. Used to differentiate from usage of console.log.
 *
 * @param args - Args to log to console.
 */
export const logToConsole = (...args: unknown[]) => console.log(...args);
